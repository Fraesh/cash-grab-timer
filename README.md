# Cash Grab Timer

A small Svelte companion timer for Cash Grab.

## Scripts

- `npm run dev` starts the local dev server.
- `npm run build` writes the static app to `dist/`.
- `npm run preview` serves the production build locally.

The app is configured with relative asset paths, a web manifest, and a service worker so the `dist/` output can be deployed to GitHub Pages.
